schema_version = "1.3"
artifacts = ["/logs/artifacts/model.patch"]
[task]
name = "benzhi/amboy-rest-idempotent-batch-submit"
description = ""
authors = []
keywords = []
[metadata]
ext_id = "khd388d48f37c8074507e4f2f6b9e2a9"
task_id = "amboy-rest-idempotent-batch-submit"
display_title = "Add idempotent, traceable batch submission over REST"
display_description = "Provide bounded, idempotent batch job submission with per-item results and status lookup, plus a Go client that shards, retries safely, and preserves input order."
original_title = "为 REST 队列提供幂等可追踪的批量提交"
category = "feature_request"
language = "go"
repository_url = "https://github.com/mongodb/amboy"
base_commit_hash = "51c8dde3a7f554122041edbe12f183b78f5ab6a9"
[verifier]
network_mode = "no-network"
environment_mode = "separate"
timeout_sec = 1800.0

[verifier.env]
[verifier.environment]
build_timeout_sec = 1800.0
cpus = 2
memory_mb = 8192
storage_mb = 20480

[[verifier.collect]]
command = "cd /app && mkdir -p /logs/artifacts && git config --global --add safe.directory /app && git diff --binary 51c8dde3a7f554122041edbe12f183b78f5ab6a9 HEAD > /logs/artifacts/model.patch"
timeout_sec = 300.0

[agent]
network_mode = "no-network"
timeout_sec = 10800.0
[environment]
build_timeout_sec = 1800.0
# docker_image is omitted in step 1: Harbor/Pier builds environment/Dockerfile.
# After the image is built and pushed, set e.g.
# docker_image = "<registry>/benzhi-swe:khd388d48f37c8074507e4f2f6b9e2a9-v1"
os = "linux"
cpus = 2
memory_mb = 8192
storage_mb = 20480
gpus = 0
mcp_servers = []

[environment.env]
[solution.env]
