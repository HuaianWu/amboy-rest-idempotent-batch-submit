Amboy 的 Queue 已支持批量写入，但 REST 服务和 Go 客户端只能逐个提交作业，生产调用方既无法高效传输大批任务，也无法在超时重试后判断哪些项目已经入队。请增加受资源上限保护、幂等且可追踪的批量提交能力，让同一提交键和内容在并发请求或响应丢失后只产生一次入队效果并可重新取得相同结果，逐项结果始终对应原输入并准确区分成功、重复、作用域冲突、无效和取消后未尝试，调用方还能查询已接受作业的后续状态；Go 客户端需要按服务限制进行有界并发分片、在安全条件下重试并按全局输入顺序汇总结果，同时保持单作业接口和不同队列实现的既有行为兼容。
